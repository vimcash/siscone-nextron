export const productActions = {
  setToggleForm: (state:any) => {
    state.toggleForm = !state.toggleForm
  }
}
export const categoryActions = {
  setToggleForm: (state:any) => {
    state.toggleForm = !state.toggleForm
  }
}